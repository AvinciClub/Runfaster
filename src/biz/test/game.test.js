import theGame from '../game';
import * as G from '../game';
import {checkin} from '../user';

test("Game event", () => {
    //checkin("Test", 0);
    //const m = jest.fn();
    //G.gameEvent.addListener(G.EVT_LOAD, m);
    //G.createGame();
    //expect(m.mock.calls.length).toBe(1);
    //expect(m.mock.calls[0][0]).toBe("test1");
    //expect(m.mock.calls[0][1]).toBe(2);
});